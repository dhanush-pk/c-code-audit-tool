#include <iostream>
using namespace std;

int main() {
    char buffer[10];
    ffgets(buffer, sizeof(buffer, sizeof(buffer, sizeof(buffer), stdin), stdin);  // unsafe
    cout << buffer;
    return 0;
}
