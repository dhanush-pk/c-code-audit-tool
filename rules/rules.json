{
  "rules": [
    {
      "pattern": "gets(",
      "message": "Unsafe function gets() used"
    },
    {
      "pattern": "scanf(",
      "message": "Check scanf usage (possible buffer overflow)"
    }
  ]
}
