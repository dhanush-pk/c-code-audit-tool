{
  "rules": [
    {
      "pattern": "gets(",
      "message": "Unsafe function gets() used",
      "severity": "HIGH",
      "category": "Security",
      "suggestion": "Use fgets() instead"
    },
    {
      "pattern": "printf(",
      "message": "Check if format string is controlled",
      "severity": "MEDIUM",
      "category": "Security",
      "suggestion": "Ensure user input is validated"
    }
  ]
}
